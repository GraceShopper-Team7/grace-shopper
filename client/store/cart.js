import axios from 'axios'

//ACTIONS

//ACTION CREATORS

//THUNK CREATORS

//REDUCER
const cartReducer = (
  state = {
    // all: [],
    // selected: {},
    // isLoading: false,
    // hasErrored: false
  },
  action
) => {
  switch (action.type) {
    default:
      return state
  }
}

export default cartReducer
